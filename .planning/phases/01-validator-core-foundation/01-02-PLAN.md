---
phase: 01-validator-core-foundation
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260521000000_initial_schema.sql
  - supabase/config.toml
  - .github/workflows/supabase-keepalive.yml
  - .github/workflows/disposable-update.yml
autonomous: false
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - INF-02
  - INF-03

user_setup:
  - service: supabase
    why: "Database schema + auth provider for all phases"
    env_vars:
      - name: SUPABASE_URL
        source: "Supabase Dashboard → Project Settings → API → Project URL"
      - name: SUPABASE_ANON_KEY
        source: "Supabase Dashboard → Project Settings → API → Project API keys → anon/public"
      - name: SUPABASE_SERVICE_ROLE_KEY
        source: "Supabase Dashboard → Project Settings → API → Project API keys → service_role (secret)"
    dashboard_config:
      - task: "Create new Supabase project"
        location: "https://supabase.com/dashboard → New project"
      - task: "Disable email confirmation for v1"
        location: "Supabase Dashboard → Authentication → Email → Confirm email → OFF"
      - task: "Add GitHub Secrets SUPABASE_URL and SUPABASE_ANON_KEY"
        location: "GitHub repo → Settings → Secrets and variables → Actions → New repository secret"
  - service: supabase-cli
    why: "Run schema migrations"
    env_vars: []
    dashboard_config:
      - task: "Install Supabase CLI"
        location: "Terminal: npm install -g supabase"
      - task: "Login and link project"
        location: "Terminal: supabase login && supabase link --project-ref <your-project-ref>"

must_haves:
  truths:
    - "Supabase project exists with jobs and validation_results tables created via migration"
    - "RLS is enabled on both tables with policies limiting access to own user's data"
    - "Supabase Auth allows signup with email + password (no email confirmation required for v1)"
    - "GitHub Action supabase-keepalive.yml triggers on schedule 0 12 * * 3 and on workflow_dispatch"
    - "GitHub Action disposable-update.yml runs npm update disposable-email-domains-js, commits only if diff exists, and redeploys via wrangler"
    - "Both GitHub Actions reference secrets SUPABASE_URL and SUPABASE_ANON_KEY"
  artifacts:
    - path: "supabase/migrations/20260521000000_initial_schema.sql"
      provides: "Full schema: jobs + validation_results tables, indexes, RLS policies"
      contains: "CREATE TABLE public.jobs"
    - path: ".github/workflows/supabase-keepalive.yml"
      provides: "Weekly ping cron preventing Supabase project pause"
      contains: "cron: '0 12 * * 3'"
    - path: ".github/workflows/disposable-update.yml"
      provides: "Weekly disposable list update mechanism (INF-03)"
      contains: "npm update disposable-email-domains-js"
  key_links:
    - from: ".github/workflows/supabase-keepalive.yml"
      to: "$SUPABASE_URL/rest/v1/"
      via: "curl -sf GET with apikey header"
      pattern: "curl.*SUPABASE_URL"
    - from: ".github/workflows/disposable-update.yml"
      to: "package.json disposable-email-domains-js version"
      via: "npm update + git diff check + conditional commit"
      pattern: "npm update disposable-email-domains-js"
---

<objective>
Set up the Supabase project foundation (schema + auth) and GitHub Actions for infrastructure maintenance.

Purpose: The schema is the data contract all phases write to. Auth setup enables user accounts required by AUTH-01..03. The two GitHub Actions provide zero-cost infrastructure maintenance: keep-alive (INF-02) prevents Supabase project pause, disposable-update (INF-03) keeps the domain blocklist current.

Output:
- supabase/ directory with migration SQL ready to push
- .github/workflows/ with 2 maintenance actions
- Checkpoint to verify schema applied and auth works live
</objective>

<execution_context>
@C:/Users/henrique.oliveira/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/henrique.oliveira/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-validator-core-foundation/01-CONTEXT.md
@.planning/phases/01-validator-core-foundation/01-RESEARCH.md

<interfaces>
<!-- Schema contracts from RESEARCH.md — implement exactly as specified -->

jobs table fields: id (UUID PK), user_id (UUID FK auth.users), status (pending|processing|completed|failed),
source (csv|manual), filename (nullable), total_emails (int), processed_emails (int),
valid_count, invalid_count, risky_count (nullable ints), input_path, result_path (nullable text),
created_at (TIMESTAMPTZ), completed_at (nullable TIMESTAMPTZ)

validation_results table fields: id (UUID PK), job_id (UUID FK jobs), email (text),
status (valid|invalid|risky), score (SMALLINT: 0|50|100),
reason (nullable: syntax|no-domain|no-mx|disposable|role|catch-all|typo),
suggestion (nullable text), created_at (TIMESTAMPTZ)

RLS policies:
- jobs: "users_own_jobs" → auth.uid() = user_id
- validation_results: "users_own_results" → EXISTS (SELECT 1 FROM jobs j WHERE j.id = job_id AND j.user_id = auth.uid())
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Supabase migration SQL and project initialization</name>
  <files>
    supabase/migrations/20260521000000_initial_schema.sql,
    supabase/config.toml
  </files>
  <action>
    Run: supabase init (creates supabase/ directory with config.toml)

    If supabase CLI not installed: npm install -g supabase

    Run: supabase login (opens browser for auth)

    Run: supabase link --project-ref &lt;YOUR_PROJECT_REF&gt;
    (Project ref is found in Supabase Dashboard → Project Settings → General → Reference ID)

    Run: supabase migration new initial_schema
    (Creates supabase/migrations/TIMESTAMP_initial_schema.sql — rename/use the timestamp as 20260521000000)

    Write the full migration SQL to supabase/migrations/20260521000000_initial_schema.sql using exactly the schema from RESEARCH.md (Pattern 7 / Supabase Schema code example). The SQL includes:
    - CREATE TABLE public.jobs (all fields with CHECK constraints)
    - CREATE TABLE public.validation_results (all fields with CHECK constraints)
    - 4 CREATE INDEX statements
    - ALTER TABLE ... ENABLE ROW LEVEL SECURITY for both tables
    - CREATE POLICY "users_own_jobs" on jobs
    - CREATE POLICY "users_own_results" on validation_results

    Run: supabase db push
    (Applies migration to the linked remote Supabase project)

    Verify: supabase db diff (should show no diff — migration fully applied)
  </action>
  <verify>
    <automated>supabase db diff 2&gt;&amp;1 | grep -E "No schema changes|Diffing" | head -5</automated>
  </verify>
  <done>
    supabase/migrations/20260521000000_initial_schema.sql exists with full schema SQL.
    supabase db diff reports no pending changes (migration applied to remote).
    Both tables visible in Supabase Dashboard → Table Editor.
  </done>
</task>

<task type="auto">
  <name>Task 2: GitHub Actions for keep-alive (INF-02) and disposable list update (INF-03)</name>
  <files>
    .github/workflows/supabase-keepalive.yml,
    .github/workflows/disposable-update.yml
  </files>
  <action>
    Create .github/workflows/supabase-keepalive.yml:

    ```yaml
    name: Supabase Keep-Alive

    on:
      schedule:
        - cron: '0 12 * * 3'   # Every Wednesday at 12:00 UTC
      workflow_dispatch:         # Manual trigger for testing

    jobs:
      ping:
        runs-on: ubuntu-latest
        steps:
          - name: Ping Supabase REST API
            run: |
              curl -sf -X GET \
                "${{ secrets.SUPABASE_URL }}/rest/v1/" \
                -H "apikey: ${{ secrets.SUPABASE_ANON_KEY }}" \
                -H "Authorization: Bearer ${{ secrets.SUPABASE_ANON_KEY }}"
    ```

    Create .github/workflows/disposable-update.yml:

    ```yaml
    name: Update Disposable Email Domains

    on:
      schedule:
        - cron: '0 10 * * 1'   # Every Monday at 10:00 UTC
      workflow_dispatch:         # Manual trigger for testing

    jobs:
      update:
        runs-on: ubuntu-latest
        steps:
          - name: Checkout
            uses: actions/checkout@v4

          - name: Setup Node.js
            uses: actions/setup-node@v4
            with:
              node-version: '20'

          - name: Update disposable-email-domains-js
            run: npm update disposable-email-domains-js

          - name: Check for changes
            id: check_diff
            run: |
              if git diff --quiet package.json package-lock.json; then
                echo "changed=false" >> $GITHUB_OUTPUT
              else
                echo "changed=true" >> $GITHUB_OUTPUT
              fi

          - name: Commit and push if changed
            if: steps.check_diff.outputs.changed == 'true'
            run: |
              git config user.name "github-actions[bot]"
              git config user.email "github-actions[bot]@users.noreply.github.com"
              git add package.json package-lock.json
              git commit -m "chore: update disposable-email-domains-js"
              git push

          - name: Deploy to Cloudflare Workers
            if: steps.check_diff.outputs.changed == 'true'
            uses: cloudflare/wrangler-action@v3
            with:
              apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
              command: deploy
    ```

    Note for user: The disposable-update action requires 2 additional GitHub Secrets beyond the keep-alive action:
    - CLOUDFLARE_API_TOKEN: from Cloudflare Dashboard → My Profile → API Tokens → Create Token (Workers: Edit permission)
    - The action only redeploys if the package actually changed — skips silently otherwise.
  </action>
  <verify>
    <automated>ls /c/Users/henrique.oliveira/Documents/Dev/.github/workflows/supabase-keepalive.yml &amp;&amp; ls /c/Users/henrique.oliveira/Documents/Dev/.github/workflows/disposable-update.yml &amp;&amp; grep "cron" /c/Users/henrique.oliveira/Documents/Dev/.github/workflows/supabase-keepalive.yml</automated>
  </verify>
  <done>
    Both workflow files exist in .github/workflows/.
    supabase-keepalive.yml has schedule cron '0 12 * * 3' and workflow_dispatch trigger.
    disposable-update.yml has npm update disposable-email-domains-js, git diff check, conditional commit and deploy.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify Supabase schema, auth flow, and GitHub Actions</name>
  <action>Human verification of Tasks 1 and 2 — follow steps in how-to-verify below.</action>
  <what-built>
    Task 1 applied the Supabase schema migration (jobs + validation_results tables, RLS policies).
    Task 2 created two GitHub Actions workflows (.github/workflows/).
    Supabase Auth is configured with email/password, email confirmation disabled for v1.
  </what-built>
  <how-to-verify>
    1. Open Supabase Dashboard → Table Editor
       - Confirm "jobs" table exists with columns: id, user_id, status, source, filename, total_emails, processed_emails, valid_count, invalid_count, risky_count, input_path, result_path, created_at, completed_at
       - Confirm "validation_results" table exists with columns: id, job_id, email, status, score, reason, suggestion, created_at

    2. Open Supabase Dashboard → Authentication → Policies
       - Confirm "users_own_jobs" policy exists on jobs table
       - Confirm "users_own_results" policy exists on validation_results table

    3. Open Supabase Dashboard → Authentication → Email
       - Confirm "Confirm email" is set to OFF (disabled for v1)

    4. Test AUTH-01 + AUTH-03 (run in browser console with your project URL + anon key):
       ```javascript
       const { createClient } = await import('https://cdn.jsdelivr.net/npm/@supabase/supabase-js/+esm');
       const s = createClient('YOUR_URL', 'YOUR_ANON_KEY');
       const { data, error } = await s.auth.signUp({ email: 'test@example.com', password: 'test1234' });
       console.log(data, error); // expect data.user to exist, error to be null
       await s.auth.signOut();
       const { data: session } = await s.auth.getSession();
       console.log(session); // expect null (AUTH-03 verified)
       ```

    5. Test AUTH-02 (session persistence):
       After signUp above, reload the page and re-run:
       ```javascript
       const s = createClient('YOUR_URL', 'YOUR_ANON_KEY');
       const { data } = await s.auth.getSession();
       console.log(data.session); // expect non-null session (AUTH-02 verified)
       ```

    6. Add GitHub Secrets to the repo:
       - SUPABASE_URL
       - SUPABASE_ANON_KEY
       Go to GitHub repo → Settings → Secrets and variables → Actions → New repository secret

    7. Test keep-alive action: Go to GitHub repo → Actions → "Supabase Keep-Alive" → Run workflow
       - Confirm workflow completes successfully (green checkmark)
       - Confirm curl exits 0 (Supabase REST endpoint reachable)
  </how-to-verify>
  <verify>
    <automated>MISSING — manual verification required; see how-to-verify steps</automated>
  </verify>
  <done>
    All 7 verification steps completed. Supabase tables, RLS policies, auth flow, and keep-alive action confirmed working.
    AUTH-01, AUTH-02, AUTH-03, INF-02 manually verified.
  </done>
  <resume-signal>Type "approved" when all steps verified, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
Full phase check for Plan 01-02:

1. Schema applied: supabase db diff → no pending changes
2. Both tables exist with correct columns (verified in checkpoint)
3. RLS enabled and policies active (verified in checkpoint)
4. Auth flow works: signUp → session → signOut → null session (AUTH-01/02/03)
5. Keep-alive action runs successfully via workflow_dispatch
6. GitHub Secrets (SUPABASE_URL, SUPABASE_ANON_KEY) configured
</verification>

<success_criteria>
- supabase/migrations/20260521000000_initial_schema.sql exists and has been pushed to remote
- jobs and validation_results tables visible in Supabase dashboard with correct schema
- RLS enabled with correct policies on both tables
- Supabase Auth allows signUp without email confirmation (v1 decision)
- .github/workflows/supabase-keepalive.yml runs successfully on workflow_dispatch
- .github/workflows/disposable-update.yml correctly diffs before committing/deploying
- AUTH-01 (create account), AUTH-02 (persistent session), AUTH-03 (signOut) manually verified
</success_criteria>

<output>
After completion, create `.planning/phases/01-validator-core-foundation/01-02-SUMMARY.md`
</output>
